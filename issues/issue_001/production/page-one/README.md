# Issue 001 Page One production

## Locked Issue 001 execution benchmark

`issue_001_page_one_polished_press_v3.png` is the editor-approved Issue 001
execution benchmark. Its canonical SHA-256 is:

```text
07d4e8332880a23b947d49ea4ea813ae558f4ada220529d63e5d37cd5bc43691
```

Preserve that PNG byte-for-byte. Do not alter, crop, regenerate, recompress, or
redesign it. The editable SVG and build script remain reproducibility aids; they
do not authorize replacing the approved benchmark with a newly rendered file.

The distinction between the two visual references is deliberate:

- `design-system/references/the-underground-press-golden-image.png` is the
  publication-wide visual canon (the Golden Image).
- This exact Page One PNG is the Issue 001 execution benchmark.

Every later Issue 001 page must both inherit the Golden Image's visual DNA and
look physically printed in the same issue and press run as this Page One.

`issue_001_page_one.svg` is the editable production master. Build the print proof with:

```text
python scripts/build_page_one.py
```

The lead reports a local crime increase and attributed accounts of coordinated
black-clad movement. “Foot Clan” remains explicitly unconfirmed rumor. The
secondary pizza-sales thread and Alice’s overheard account add evidence without
solving the mystery. Reader-facing copy follows Sliding Time and the anonymous
newsroom credit.

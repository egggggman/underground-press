# Design System

Reusable visual foundations, layouts, widgets, and page furniture live here.

The active production sources are `components/`, `tokens/`, and the benchmark registry in `references/visual_benchmarks.json`. Material under `archive/` is retained as historical evidence and must not be used as current production truth.
